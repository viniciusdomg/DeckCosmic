import 'package:flutter/material.dart';

class HeroDay extends StatelessWidget {
  const HeroDay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
